<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Movies" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="BackgroundApp">
        <item name="android:background">@color/teal_200</item>
    </style>

    <style name="TextColorApp">
        <item name="android:textColor">@color/cor_geral</item>
    </style>

    <style name="TextColorSecondaryApp">
        <item name="android:textColor">@color/cinza</item>
    </style>

    <style name="VectorAssetColor">
        <item name="tint">@color/azulApp</item>
    </style>

    <style name="SearchViewTheme">
        <item name="colorControlNormal">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="android:editTextColor">@color/white</item>
    </style>

    <style name="SeekBarTheme">
        <item name="android:progressBackgroundTint">@color/color_primary_dark</item>
        <item name="android:progressTint">@color/colorBarraCarregamento</item>
        <item name="colorSecondary">@color/color_primary_dark</item>
    </style>

</resources>